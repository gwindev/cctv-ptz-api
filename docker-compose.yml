services:
  ptz:
    build: .
    image: ptz
    container_name: ptz
    ports:
      - "5001:80"
    environment:
      - STREAMIN_BASE_URL=http://mainapi:80
    volumes:
      - ./presets.json:/app/presets.json
    restart: unless-stopped
    # ไม่ต้องกำหนด networks จะใช้ defaultk  